{
  "name": "campuscoder",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "start": "npm --prefix frontend run start",
    "start:standalone": "npm --prefix frontend run start",
    "lint": "npm --prefix frontend run lint",
    "type-check": "npm --prefix frontend run type-check",
    "test": "npm run lint && npm run type-check",
    "test:unit": "npm --prefix frontend run test:unit",
    "production-check": "npm run lint && npm run type-check && npm run build && npm --prefix backend run lint && npm --prefix backend run type-check && npm --prefix backend run build",
    "test:e2e": "playwright test",
    "doctor": "npx react-doctor@latest"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@tailwindcss/postcss": "^4",
    "@testing-library/dom": "^10.4.1",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.2",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "@types/three": "^0.184.1",
    "@vitejs/plugin-react": "^6.0.2",
    "eslint": "^9",
    "eslint-config-next": "16.2.6",
    "jsdom": "^29.1.1",
    "lighthouse": "^12.6.1",
    "react-doctor": "^0.2.14",
    "tailwindcss": "^4",
    "typescript": "^5",
    "vitest": "^4.1.8"
  },
  "dependencies": {
    "next": "15.5.23"
  }
}
